version: "3.9"

services:
  # ── Production ───────────────────────────────────────────────────
  web:
    build:
      context   : .
      dockerfile: Dockerfile
      target    : runtime
    container_name: cv-match-web
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=production
      - FLASK_DEBUG=0
    restart: unless-stopped
    healthcheck:
      test    : ["CMD", "python", "-c",
                 "import urllib.request; urllib.request.urlopen('http://localhost:5000/')"]
      interval: 30s
      timeout : 10s
      retries : 3
      start_period: 15s
    networks:
      - cv-net

  # ── Development (hot-reload) ──────────────────────────────────────
  web-dev:
    build:
      context   : .
      dockerfile: Dockerfile
      target    : builder
    container_name: cv-match-dev
    command   : python app.py
    ports:
      - "5001:5000"
    environment:
      - FLASK_ENV=development
      - FLASK_DEBUG=1
    volumes:
      - .:/app          # mount seluruh folder untuk hot-reload
      - uploads_vol:/app/uploads # Volume mount untuk uploads agar tidak hilang saat reload container
    networks:
      - cv-net
    profiles:
      - dev             # aktifkan: docker compose --profile dev up

volumes:
  uploads_vol:

networks:
  cv-net:
    driver: bridge
